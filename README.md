local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local Config = { Active = { BallControl = true }, Values = { BallControlDistance = 2.5 } } local function GetBall() return workspace:FindFirstChild("Ball") or workspace:FindFirstChild("Football") or workspace:FindFirstChild("SoccerBall") end local function BallControl() if not Config.Active.BallControl then return end local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") local ball = GetBall() if not ball or not root then return end pcall(function() ball.CFrame = root.CFrame * CFrame.new(0, -1, -Config.Values.BallControlDistance) ball.AssemblyLinearVelocity = Vector3.zero ball.AssemblyAngularVelocity = Vector3.zero end) end -- Execução em loop game:GetService("RunService").RenderStepped:Connect(BallControl)
